apiVersion: 1

providers:
  - name: ContentFlow
    orgId: 1
    folder: ContentFlow
    folderUid: contentflow
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
