# ContentFlow 执行纠偏与短交接

日期：2026-09-24。用途：响应用户对反复审计、消耗额度而未持续实施的质疑；不是新的项目审计，也不是新的开工授权。新任务已经接手实施，当前断点见第 6 节，不要把下列历史审计请求恢复为当前目标。

## 1. 已核实的过程问题

最近三个实施回合都在 contextCompaction 后切回“只做全面审计”：

- `01a0d06c-1903-7291-8b12-2e3318ac3a11`：A41 新增 20 项回归通过后，转为全项目审计。
- `01a0d088-ec03-71f3-a5ea-17cef7af20d8`：A41/A53/A54 升级顺序、版本核验、容器入口实施后，转为全项目审计。
- `01a0d0b5-3b59-7e31-ab2d-eb8e8539e06a`：开始 A55/A56 日志修复后，转为全项目审计，随后额度限制使回合失败。

证据来源：当前任务 read_thread 的 agentMessage、contextCompaction 和回合状态。能确认行为切换与压缩事件相邻，不能据此断言底层模型或平台存在特定缺陷。直接执行责任在助手：没有正确恢复当前实施状态，把旧审计请求重新作为当前执行模式，甚至把本轮刚做的修改重新当成“接手已有候选”。

其他放大因素：缺少阶段关闭条件、重复复现已知反例与重跑全套、历史交接过长、真实环境验收与本地修复混为一条无限等待链。用户授权或网络问题不能解释这些本地实施停滞。

## 2. 已有成果与当前断点

- 最近已核实本地 HEAD：`628bb0a`，分支 `codex/enterprise-media-runtime`。
- `12eaf9a`：浏览器会话上下文与续期协调；`fe6d0e3`：Worker 执行保护与操作恢复；`9a1da99`：存储 staging 和旧素材结果隔离；`628bb0a`：相关验收记录。以源码/提交为证，不把它们等同于真实部署。
- 未提交：A41/A53/A54 数据库准入、显式迁移、停写备份和 Embedding 模式分流；A55/A56 安全异常诊断、ASGI 错误边界及日志配置修复。保留全部现有文件，不从 HEAD 重做。
- 日志相邻套件本地 `196 passed / 1 warning / 17 subtests`。随后带早停的全套为 `316 passed / 4 failed / 29 subtests`，不是全绿。
- 失败包括：生成接受原子性测试仍等待 RuntimeError，而新边界返回 500；三个素材来源漂移子用例丢失安全、明确的配置处理提示。第一项需改为 HTTP 契约并继续验证数据库无残留，不能删掉原子性断言；后三项需提供稳定错误码和安全处理指引，不能恢复原始异常泄露。
- 后续 `--maxfail=0` 全套曾启动，旧 session `73559` 已不可用；未取得最终结果，不冒记通过或结束。再次开工先核对是否仍有进程，不盲目重复启动。
- 最新前端模块/SSR `40/40`、构建、ESLint、无增量 TypeScript、Ruff 通过。最后申请的 Edge 运行因自动审批服务额度错误未执行，不沿用旧 44 项作为新结果。

## 3. 建议的新执行方式（历史建议；现已选择新任务）

1. 一个阶段只做一个可关闭的实施包。当前先收尾已有候选和上述失败，不重新全面审计，不同时启动下一包。
2. 每项沿“已知失败 → 实现 → 针对性回归 → 阶段验收”推进。新发现的非阻断问题只入待办，不立即扩展范围。
3. 先针对性回归；阶段结束再跑一次全套。未改变实现、夹具或环境时，不重复跑同组来证明同一事实。
4. 阶段完成标准：实现已存在，相关失败已解释并修复，验证结果明确，记录简短；符合既有同步授权时普通提交/推送。真实 PG/MinIO/部署未验收单列，不能冒称完整交付。
5. 下一实施包：统一媒体有效性与完整发布字段审核；再做预算、部分成果保存与配置身份。不要因又生成一份报告就算实施完成。
6. 若新开任务，先由用户暂停旧持续目标，保持一个写入者。新任务直接使用当前仓库，保留未提交修改，不复制整段旧聊天。不要同时重做已存在实现。

## 4. 不变的权限边界

不 reset、覆盖、强推或部署；不读/哈希/修改/暂存 `knowledge/北京周末 CityWalk 路线助手产品资料.txt`。不操作真实账号、模型费用、平台发布、业务数据库、Ubuntu、代理或凭据。真实调用和在线扫描的授权缺口不能绕过。当前复盘未修改业务代码、提交或推送，也未创建新任务或更改持续目标状态。

## 5. 新任务可用起始指令

> 接手 ContentFlow 的当前工作树。先读 docs/EXECUTION_RESTART.md、docs/IMPLEMENTATION_PROGRESS.md 末节和 git status，保留全部未提交修改。本任务是实施，不是重新全面审计。先收尾 A41/A53/A54 与 A55/A56 在制候选，优先处理日志修复引入的 HTTP 测试契约和安全错误提示退化。先针对性验证，阶段收尾再执行全套；没有实现变化不要重复泛审和全套。新增非阻断问题只记录。遵守现有真实服务、费用、凭据和受保护文件边界；遇到确需用户补充的权限/环境才停。以一个可验收、可同步的阶段结果结束，不以“建议下一步修复”结束。

## 6. 新实施任务当前断点

- 已按用户选择直接接回原工作树；本任务独立协作，不向旧任务索取指令，也不自行创建持续目标。
- 第一阶段 A41/A53/A54 与 A55/A56 已完成本地实施收尾。已修 HTTP 500 原子性回归与三个素材来源漂移子用例，并补异步轮询固定错误码/安全指引和日志入口回归。
- 针对性验证已通过 78 项/50 子用例，以及数据库/升级/日志入口 80 项/6 PG 跳过；最终后端 **970 passed / 59 skipped / 228 subtests**、本次独立 Edge **44/44**。完整边界、警告与同步见 [实施进度](IMPLEMENTATION_PROGRESS.md) 末节及本功能分支提交历史。全部验证句柄已终结；不要重跑没有变化的同组，也不要引用旧 session 73559 为通过。
- [数据库契约](database_schema_contract.md) 与 [安全诊断契约](safe_diagnostics_contract.md) 是本阶段边界。真实 PG/MinIO/恢复、A33 指标冲突与下一包媒体/最终审核问题独立开放；第一阶段结束后由用户决定下一阶段。
