# 外部服务真实验收记录

本文只记录经过授权的真实外部调用结果。所有证据均脱敏，不保存 Access Token、App Secret、平台原始响应或可用于登录的标识。适配器存在不等于真实账号已验收；“草稿已创建”也不等于“内容已公开发布”。

## 验收状态

| 服务与能力 | 状态 | 最近证据 | 尚未证明 |
| --- | --- | --- | --- |
| 微信公众号鉴权与只读计数 | 已验证 | 2026-08-09 取得有效令牌；素材与草稿计数接口成功 | 限流、令牌过期和权限撤销矩阵 |
| 微信公众号永久图片素材 | 已验证 | 受控生成 1 张测试 PNG，图片素材数 15 → 16 | 素材删除、超规格与格式拒绝 |
| 微信公众号“不发布”草稿 | 已验证 | 标记 `CF-20260809-1443-9209E2`；草稿数 5 → 6；连接器返回 `draft_created` | 公开发布、审核拒绝和最终文章对账 |
| 微信公众号公开发布 | 未执行 | `auto_publish=false`，确认未调用 `/cgi-bin/freepublish/submit` | `publish_id`、`article_id` 和自动对账 |
| 抖音企业发布/指标 | 待账号 | 企业账号仍在申请，当前不开展真实调用 | OAuth、上传、发布、回调、指标 |
| 小红书自动发布 | 不支持 | 当前坚持审核后 ZIP 导出 | 仅在取得官方开放平台资质后重新评估 |
| 外部模型与媒体 Provider 真实生成 | 待验收 | Media Contract v1 与受保护 live conformance runner 已就绪；尚未配置目标服务，未发生真实调用 | 目标服务生成、同键不重复计费、时延/错误/审核/过期、Token/成本与质量 |

## 2026-08-09 微信公众号受控写入

- 授权范围：用户明确允许创建测试永久素材和草稿，且暂不公开发布。
- 前置检查：白名单调整后，Access Token、素材计数和草稿计数均成功。
- 写入路径：通过真实 `WechatConnector.publish`，使用内存生成的测试 PNG，渠道配置强制 `auto_publish=false`。
- 可观察结果：图片素材增加 1，草稿增加 1，连接器状态为 `draft_created`。
- 安全断言：调用跟踪确认没有请求公开发布提交端点；没有输出或写入任何凭据。
- 外部副作用：该永久图片素材与草稿当前保留在用户公众号后台。后续如需清理，应由有权限的管理员在平台后台核对标记后删除，不能在未确认目标的情况下批量删除。

## 使用规则

1. 后续真实调用先更新本表的授权范围和预期副作用。
2. 每种能力分别记录成功、超时、限流、凭据过期、权限不足、平台拒绝和不确定结果；不能用单次成功替代异常矩阵。
3. 日志和文档只保存摘要、计数变化、平台错误码类别和追踪哈希，不保存秘密值或完整内容。
4. 公开发布、删除线上素材、改变账号配置等高风险动作必须再次取得明确授权。
5. 媒体 conformance 会产生真实图片/视频并可能计费；只有在目标 Base、Key、模型名和下载域名明确后运行。报告只保留脱敏指纹；自动通过不能替代账单核对、人工质量抽检和故障注入矩阵。
