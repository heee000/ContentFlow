# ContentFlow 持续改进实施进度

更新：2026-09-24。目标：按 [全面复审计划](project_audit_20260921.md) 逐项实施并验证，保持私人测试范围；不以本次已完成的局部工作替代完整目标。真实付费调用、内容审批/发布及高影响权限迁移仍按具体授权推进。

## 当前阶段

**最新只读复审（源码 `fe6d0e3`）**：见 [执行权候选后的全项目再审](project_audit_20260924_execution_review.md)。本轮相关正式回归 72 passed / 4 PG skipped；新增 **A51 慢存储事务阻塞自身续租**，实际素材 Worker 在隔离 SQLite 复现写出文件但任务/素材/账本未完成，等耗时发生在存储事务前的正对照成功。A47/A39/A40/A33/A35/A41/A46/A34/A16/A28 亦重新复验。当前本地 HEAD 已为 `fe6d0e3`，以下 `16c26bf`/未提交说法属于此前阶段记录，不代表当前源码。当前候选仍未取得同版真实服务/部署签收；本轮只更新审计记录，没有业务改动或 Git 同步。

**后续执行权阶段（A07）已完成本地完整回归**：独立领取令牌、逐调用检查、业务事务写入保护、迟到调用证据、过期续租拒绝与 SQLite 原子领取已实现；完整后端 **847 passed / 50 skipped / 226 subtests（383.04 秒）**，当前 API/迁移下完整 Edge **43 passed（1.9 分钟）**，执行权/迁移专项 40 passed。新增 head **e9f0a1b2c3d4**，真实 PG 四项新竞争、服务级/同版 CI 和 Ubuntu 部署仍未签收。详情见末节与 [执行权契约](worker_execution_contract.md)；以下发布阶段结果是前一批证据，不代替当前完整回归。公网冻结、真实调用授权和依赖外发边界不变。

最新审计入口为 [2026-09-24 发布事务候选与全项目复审](project_audit_20260924_acceptance_review.md)，原始失败证据保留。**后续已完成 A49 发布回执、A50 UTC 响应的本地修复，连同已有 A05/A45、A44/A48 仍待真实服务与同版签收**：完整后端 806 passed / 46 skipped，完整 Edge 浏览器 43 passed，默认模块/SSR 40 passed；最后补充的响应模型覆盖检查与回执专项 10 passed / 3 skipped，详情见末节。本地不再沿用“1 项后端失败、3 项回执失败”的旧结论，也不把本地绿色说成 PG/MinIO、CI 或 Ubuntu 已通过。当前 HEAD 仍为 `16c26bf`，源码未提交、未部署；不复用 `12eaf9a / CI 35904373071` 的绿色。在线依赖审计授权仍待明确，先记录本地证据与交付边界，再按执行权、费用、成果保存和完整计划推进；目标未完成。

最新复审见 [2026-09-23 再次全链路复审](project_audit_20260923_followup.md)：该轮只审计，新增 A30–A35。持续目标随后恢复实施，A30/A31/A32 的本地安全修复已通过最终回归，见 [渠道安全契约](channel_security_contract.md) 和本页末节。没有部署；A33/A34/A35 与完整计划继续保留，不能将本次安全阶段当作整个目标完成。

2026-09-23 的复审补充 A25–A28 后，已按持续目标恢复实施；详见 [复审快照](project_audit_20260923.md)。A01/A02/A03/A27/A29 已普通推送为 `942faa5` 并取得真实服务 CI 证据；随后 A30–A32 为 `a1e828f`，A25/A26 确认与回执收尾为 `0d6608f`。最后一批 CI 四项全过：623 passed / 226 subtests、覆盖率 84.56%，另 6 条 Chromium 旅程通过。**这些新版本均未部署到 Ubuntu**，真实用户/平台签收仍保留。下一步 A04/A06 所见即所批与审核版本、A05 幂等及后续阶段；不能把局部修复当作完成整个目标。

| 项目 | 当前状态 | 验收/后续 |
| --- | --- | --- |
| A01 素材未知门禁 | 已实现共享领域检查、关联历史风险、API 锁顺序和审核者核对入口 | 本地与真实 PostgreSQL 并发 CI 通过；部署与双页面交互验收待签收 |
| A02 人工/AI 来源 | API 保留显式来源；Worker 在人工/其他 provider 配置漂移时不调用供应商、明确失败；来源矩阵和新增专项通过 | CI 通过；更完整的模型/端点快照仍属 A09；部署待签收 |
| A03 严格模型输出 | 已实现策划/成稿/审核契约 v1、错误脱敏、原调用证据保留和 Eval 契约版本门禁 | 相关专项、本地全套及真实服务 CI 通过；真实新版契约需部署后受控评测，旧六例不得直接当作新证据 |
| A27 下载锁顺序 | Worker 与 API 已统一先锁内容再锁素材；新增真实 PostgreSQL 下载/改稿竞争测试 | 本地与真实 PostgreSQL CI 通过；部署待签收 |
| A25 最终发布物 | 确认凭证/权限/过期/改图字节回归和真实页面预览门禁已 CI 签收 | `0d6608f` 的 API/PG/Edge/Chromium 验收通过；未部署、真实用户/平台待签收 |
| A26 发布幂等 | 操作 ID 绑定和等待锁后重查回执已 CI 签收；不能据此关闭所有接受/恢复故障 | `0d6608f` 的 PG 竞争和正常失回执恢复通过；本轮新增 A48 SQLite 事务和 A49 存储失败退路，未部署 |
| A28 去除 Demo 领域硬编码 | 待实施 | 中立 CTA/规则修复/视觉方向，多行业回归 |
| A29 存储扫描时钟边界 | 已复现并修复；固定/回拨时钟、单页/分页均保留真实缺失检测 | 存储 18 项、本地全套及 CI 通过；部署待签收 |
| A04 未保存审批 | 已实现 dirty/保存中门禁、回执丢失与远端冲突输入保护，未部署 | 本地 Edge/CI Chromium 19 条完整浏览器集通过，其中 11 条审核旅程 |
| A05 生成幂等 | 已实现操作编号、摘要、回执唯一约束、Brief 前置条件和网页原请求恢复 | 本地回归通过；五项 PG 竞争和同版 CI 待签收，不是供应商 exactly-once 或费用上限，未部署 |
| A06/A38 审核版本与证据 | 已实现版本/hash、旧证据归档和本地复核；后端/SQLite 迁移/回滚及 PG 四种竞争通过 | 审核迁移 b6c7d8e9f0a1，d6596d5 CI 签收；当前迁移 head e9f0a1b2c3d4，实机协调升级待授权 |
| A07 执行权与迟到结果 | 已实现领取令牌、调用前/返回后检查、事务内写入门禁、过期续租拒绝与迟到证据保留 | 本地执行权/迁移 40 项通过；PG 四项竞争及同版 CI 待签收，未部署，不承诺撤回在途远端调用 |
| A09/A12/A14 调用可控余项 | 待实施 | 完整执行配置快照、预算和未知处置语义；跨页续期 A08 见 A37 |
| A10/A11/A13 内容质量 | 待实施 | 相关性/引用/索引版本、批处理、真实任务与人工质量评测 |
| A15/A22/A23 运行验收 | 部分测试资产已有，目标环境待签收 | E2E、异机恢复、通知、峰值与当前主机封面/微信链路 |
| A16/A17/A24 体验与可维护性 | 本进度页提供当前状态入口，其余待实施 | 按需加载、URL/草稿、模块拆分及版本事实对齐 |
| A18/A19/A20/A21 扩大开放的安全边界 | 尚未实施，不自动恢复公网计划 | 数据生命周期、DB 最小权限/RLS、身份密钥、网络出口；高影响项需明确授权与恢复前置 |
| A30/A31/A32 新安全缺口 | 修复已普通推送为 `a1e828f`：受控错误/日志、固定官方目标、严格平台配置、历史错误响应隔离 | CI 四项通过，578 passed、84.33%；未部署；历史实机日志及凭据影响尚未核查 |
| A33/A34 正确性缺口 | 累计指标、重复/空平台仍复现；显式 null 已修复并在上一批签收 | 指标口径、非空去重/上限及活动并发编辑仍需整改；A45 已阻止无效平台进入生成 |
| A35 Embedding 响应对应关系 | MockTransport 复现重复/越界 index 被接受，未修复 | 严格索引类型、唯一性、完整输入覆盖；异常不替换可用索引 |
| A36 文本重定向 | 独立 opener 禁止跳转，15 项离线跳转回归及本批 CI 通过 | 已普通推送、未部署，不代表全部出网治理已完成 |
| A37/A08 多页工作区与续期 | 已有服务端上下文门禁、固定请求代次、跨页 Cookie 协调、旧响应丢弃和输入保留 | `12eaf9a` 本地 Edge/CI Chromium 27 条、14 项模块/SSR、真实 PG 4 项新行锁竞争通过；未部署，API/Web 必须协调升级 |
| A39/A40/A41 | 无效 AI 图片 ready、规则遗漏发布字段、空 schema 仍 ready 均隔离复现 | 统一媒体校验、最终发布字段规则及迁移就绪门禁 |
| A42 非有限指标 | 已实现 API/Worker 数值边界、DB 约束、历史无损隔离和前端故障隔离，未部署 | SQLite/API/Worker/页面及真实 PG 边界/NaN/旧数据迁移通过；A33 累计口径仍开放 |
| A43/A15 测试边界 | 已改显式工厂启动、测试导入前隔离、稳定动态名称定位 | 四项初始化/生命周期探针、全套与本批 CI 通过；不等于裸脚本自动隔离 |
| A44/A46 正确性短路径 | A44 已有定向唯一键插入，冲突保留调用者修改及本地全套通过；A46 Mock override 绕过仍复现 | A44 的 PG 两会话竞争/同版 CI 未签收；A46 仍待实现，不合并判作已修 |
| A45 空目标假成功 | 已实现 API 入队前和 Worker 构造 Provider 前非空/子集/去重门禁 | 本地实际调用链回归通过，同版 CI 待签收；不代表 A34 所有活动字段已规范化 |
| A47 生成检查点 | 第一平台成功后第二平台失败，已完成稿件未持久保存 | 按阶段/平台保存、部分成功与安全续作；未知供应商结果不能自动重放 |
| A48 发布接受原子性 | 原子插入、入队/审计故障无残留、孤立回执拒绝和本地全套通过 | A50 本地退步已修；PG/同版 CI 待验，禁止自动补发旧孤立记录 |
| A49 发布回执存储 | 已实现严格状态、保存读回、身份匹配清除、只读核对、30 秒截止与人工处置入口 | 默认模块含 16 项专项，浏览器发布恢复 13 项通过；非跨设备持久恢复、不保证供应商 exactly-once，未部署 |
| A50 发布时间契约 | 响应模型统一带时区 UTC；首次接受/重放/查询/列表保持时刻，原后端失败已修 | UTC/上海/纽约冬夏四条真实页面旅程通过；PG 时间断言尚跳过，未部署，不追改历史时间 |

## A01/A02 本轮设计要点

- 所有用户发起的素材重试、来源切换、候选下载、已有素材替换、内容重新审批和通用 Job 重试复用检查。未解决人工核对、活动任务、未核对的生成/轮询账本分别处理。
- 风险记录跟随同一内容的素材历史，正文改版不能把旧 unknown 藏起来；活动任务只约束当前素材，正常混合候选可并行。
- API 统一先锁 ContentItem，再锁 Asset，随后锁 Job；等待后重新读取，防止旧状态通过。新增真实 PostgreSQL 的重试/切来源竞争测试。
- `request-manual-review` 仅审核者可用，只把已终止素材生成/轮询失败转入核对，不声称已核实，也不调用供应商；核对后复用原 Job 重试，不另开双份任务。历史 attempt 原样保留。
- 下载/搜索是只读操作，不将其失败误当作新的生成费用；保留媒体契约的同键自动恢复，人工重新操作仍受门禁保护。
- 修正复审的幂等证据表述：本地账本键与实际媒体生成幂等键不同，新 Job 并不必然导致重复扣费；供应商履约仍需外部验证。
- 来源测试覆盖 12 种 manual/generate/search/hybrid × manual/http/mock，以及人工上传后编辑、人工视频来源和 AI 配置缺失；无真实媒体调用。
- 原有完整流程测试依赖“默认 manual 却自动生成”的旧缺陷，现明确声明 `image_source=generate`，保留实际生成/导出断言，不删断言来制造通过。

## 验证记录

- 素材/候选/核对首轮：17 passed / 14 subtests；扩展来源用例后 manual 模块 14 passed / 14 subtests。
- Ruff、前端 ESLint、Next.js 生产构建/类型检查及 Vinext/2 项 SSR 测试通过。第一次 Next 构建受沙箱 `.next/trace` 写权限限制，正常权限下同命令通过；未修改 ACL。
- 初次全套：398 passed / 18 skipped / 215 subtests，1 个依赖旧默认来源行为的流程测试失败；明确测试来源后，复跑最终结果为 **401 passed / 18 skipped / 215 subtests passed**（含后续增加的来源用例）。
- 本机 Docker daemon 未运行，PostgreSQL/MinIO 测试本地跳过；跳过不能算通过，待 CI 取得真实服务证据。
- 没有变更真实数据库、密钥、代理、当前待审稿或部署。受保护知识文件不读不改。

## 2026-09-23 后续实施补充

- 素材专项：38 passed / 17 subtests；覆盖排队后的 http/manual/mock 配置漂移，确认 Worker 在建 Provider 前拒绝、任务终止失败且原来源保留。
- 三阶段输出/Eval/Worker 专项：81 passed / 2 subtests。后续增加了 Eval 契约版本门禁，进入全套回归。
- 不把 `"false"`、整数 1 当 bool；不把 NaN/Infinity 变成 10 分；缺字段、非法引用结构和平台布局会失败。详情见 [输出契约](model_output_contract.md)。
- 测试与生产一致性修正：候选夹具使用生产 `candidate_optional` 名称；Mock 公众号在没有事实/必含信息时给出非空、明确人工确认的段落摘要。静音镜头允许空口播，不为测试通过捏造语音。
- 重要升级约束：旧真实 Eval 仍保留但没有契约 v1 证据；部署后新生成需另行受控评测。当前未部署、不重跑真实模型、不代替用户审批现有稿件。
- 扩展全套初轮为 456 passed / 19 skipped / 218 subtests，另有 1 个存储核对失败。没有以单独重跑通过掩盖它：新增固定/回拨时钟 × 单页/分页四个失败回归，修复已见对象的扫描标记严格晚于扫描起点；存储 18 项通过。
- 修复后的最终本地全套：**461 passed / 19 skipped / 7 warnings / 218 subtests passed，104.44 秒**。19 项跳过不当作真实服务通过；警告来自现有测试客户端兼容层和 SQLite 日期适配。真实 PostgreSQL 并发及 MinIO 等以本轮提交的 CI 结果另行签收。
- `942faa5` 的 [GitHub CI 35844426448](https://github.com/heee000/ContentFlow/actions/runs/35844426448) 四项全部通过：**480 passed / 7 warnings / 218 subtests，163.06 秒；覆盖率 83.61%**。Python/npm 当次依赖扫描未报告已知漏洞，前端生产构建、SBOM/来源签名校验通过；不等于渗透测试、实机部署或真实平台验收。

## 2026-09-23 A25 服务端发布物绑定

- 新增 [发布物绑定契约](publish_manifest_contract.md)。新任务必须素材就绪且具备工作区内 URI/SHA-256/大小；记录正文、选用素材和渠道配置摘要。Worker 检查后固定脱离 Session 的副本，实际读取素材时核对同一批发送字节。
- 官方、脚本与导出共用；安全重试/脚本回退不能重新捕获不同发布物；无 manifest 的旧排队任务拒绝，已提交/终态仍先返回，不触发重复调用。
- 专项首轮 38 passed / 8 subtests；随后本地全套 **470 passed / 20 skipped / 7 warnings / 226 subtests，108.10 秒**。之后追加微信 MockTransport 字节校验测试并把换图夹具改为不同文件，最终相关专项 **39 passed / 8 subtests，21.86 秒**；没有实际网络请求。Ruff、diff 检查及前端 lint 通过。本批最终 CI 另行签收，不复用上一提交结果。
- 新增 PostgreSQL 排期/选图竞争用例；本机跳过，必须 CI 签收。测试夹具补齐真实临时存储对象/校验和，脚本失败夹具保留原 request_json，仅改所模拟的字段，不删生产校验制造通过。
- 尚未实现最终预览与确认指纹，不能保证用户看到的页面就是服务端捕获的版本；A25 仍部分完成。所有修改均未部署，真实待审稿和微信未触碰。
- `f399c0f` 首次 CI `35846018526`：490 passed / 1 failed / 226 subtests，覆盖率 83.92%。失败为既有素材重试/切源竞争用例把所有 Job 总数断言为 1；夹具升级成真实存储对象后，切源胜出还会正确排队一个旧文件 `storage.delete`。保留两个入口必须一胜一拒及仅一个生成任务断言，新增清理任务仅在切源胜出时出现、引用确切旧对象/owner 的校验；不以重跑偶然胜出掩盖问题。此次前端与 SBOM 已过，但整体失败、来源签名跳过，修正后须新 CI 签收。
- 最终源码验证提交 **`69d27ac622acefa3abd1bada5f5f71661efc1c25`**，已普通推送。[CI 35846656631](https://github.com/heee000/ContentFlow/actions/runs/35846656631) **四项全部通过：491 passed / 7 warnings / 226 subtests，171.24 秒，覆盖率 83.92%**。包含新增 PostgreSQL 排期/选图竞争及真实 MinIO；前端 lint/SSR/生产构建、当次 Python/npm 安全扫描、SBOM 和来源签名校验通过。另本地素材/存储专项 33 passed / 17 subtests。最后只补文档签收，不把该 CI 冒称为部署或浏览器交互验收。

## 2026-09-23 A30/A31/A32 安全边界实施

- 先新增 50 项正式回归，旧实现全部失败：非法 bool、任意渠道地址、微信鉴权/上传/草稿/提交/查询阶段的 HTTP/网络/JSON/平台错误及显式重定向。采用 MockTransport，无真实网络/平台调用。
- 新增分平台封闭配置，新请求严格校验，旧渠道在连接测试/预览/发布加载/运行时重新校验。微信只有 JSON true 才公开提交；页面显示异常配置而非安全草稿提示。官方 Origin 固定、所有请求禁止跟随重定向，不保留测试域名例外。
- 连接器仅提供受控阶段/类别/数字状态码，去掉原响应/异常链；敏感 httpx 请求日志不输出 token URL。Worker 四类平台任务统一兜底，历史错误不原样返回，但数据库证据不删除。安全诊断仍可显示 HTTP 状态和白名单提示。
- 保持外部写入前安全重试、写入后结果不确定人工对账的区别；新增安全异常同样进入原未知结果门禁。无效旧配置不排队/不发网络，不自动修正配置或更新旧发布清单。
- 扩展覆盖抖音、API→Worker→viewer、错误数据库/日志、历史证据保留、正常真假语义、实例存活期间配置变化及任务删除竞态。现有标量发布 ID 兼容保留；不接受对象/布尔型 ID 或数字 token。
- 中间验证：安全＋原连接器/清单 60 passed；安全/API/Worker/脚本流 115 passed / 8 subtests；扩展安全专项 86 passed。首轮全套 556 passed / 20 skipped / 7 warnings / 226 subtests，140.62 秒；随后补充的最终源码仍需本页最后签收，不复用中间绿色。Ruff、前端 lint/TypeScript、diff 检查通过。
- 最终源码本地全套 **558 passed / 20 skipped / 7 warnings / 226 subtests，181.66 秒**；安全专项＋原连接器/素材清单 **97 passed，6.96 秒**。Ruff、前端 lint、TypeScript、Next.js 生产构建、diff 检查通过。20 项跳过不算真实 PG/MinIO 通过；本轮没有更新覆盖率或在线依赖扫描结论。中间 556/557 次全套后有补充保护/标量 ID 兼容，以上最终结果才对应定稿源码。
- 现有 A25/A26 在制代码已完整保留。已核对依赖齐全、作者 John Wang/heee000 noreply 和登录账号 heee000，按阶段同步授权普通同步当前功能分支快照，明确标注在制，不以提交替代最终预览/幂等专项、PG 并发和浏览器验收。不是 GitHub 登录或强推故障；无 force、合并、真实费用、部署、网络/密钥修改。完整目标继续 active。
- 实际源码提交 **`a1e828f1dedf9826dd0441c2d0332a1a441437c0`** 已成功普通推送到 `codex/enterprise-media-runtime`，没有开放 PR、合并或部署。仅触发一次 [CI 35854697647](https://github.com/heee000/ContentFlow/actions/runs/35854697647)，已按该确切 ID 复核为 **in_progress**（源码 SHA 一致），尚不能写成通过。继续时查询同一 run，不因列表短暂未显示而重复 dispatch。最后仅补本记录，CI 的源码证据仍指向 `a1e828f`。
- 后续按同一 run 签收：四项全部 success，**578 passed / 7 warnings / 226 subtests，170.95 秒，覆盖率 84.33%**；含真实 PostgreSQL/MinIO，前端构建、安全扫描和来源证据通过。该结论只对应 `a1e828f`，不覆盖下面的新源码。

## 2026-09-23 A25/A26 确认协议与浏览器验收

- 新建 41 项确认专项。先确认失败证据：签名版本 bool/float 被接受；预览不要求操作编号、原签名换编号可创建第二份任务。修复为严格版本和签名绑定操作编号，同步更新验证脚本与旧夹具。
- 原路径字节变化测试首轮错用内容寻址的 `put()`，实际生成新 URI；没有将夹具错误算作漏洞。改为只在临时夹具中损坏确切对象，覆盖同大小篡改/变大/缺失均被预览读取拒绝。最终发送仍由既有逐字节校验保护，不宣称确认事务提前读取了所有素材。
- 同键回执重放优先于过期/当前状态校验；新增内容锁等待后的回执重查。四种 PostgreSQL 测试覆盖同请求、不同模式、不同内容和胜者提交前改稿，断言仅一份新 PublishJob、一个分发 Job、一次确认审计；本机未配置 PG，必须本批 CI 签收。
- 本地确认＋渠道安全专项 **128 passed / 21 skipped，25.59 秒**；最终后端全套 **599 passed / 24 skipped / 7 warnings / 226 subtests，199.34 秒**。24 项跳过不算真实服务验证。Ruff、前端 lint/TypeScript、Next 生产构建和 diff 通过；SSR 与最终 CI 另行记录，不混用结果。
- 新增 Playwright 1.63.0 锁定依赖及 [浏览器验收入口](browser_acceptance.md)。真实 Next/API/临时 SQLite、无 Worker，六条 Edge 无头旅程 **6 passed，25.9 秒**；确认前封面/勾选门禁、服务端入库后丢回执与刷新原样重放、改稿冲突、存储拒绝、素材失败和跨账号隔离均通过。首轮是测试 `getByLabel` 包含 option 文本导致定位失败，改为真实无障碍 combobox 名称；未修改产品来绕过失败。
- 本机默认 Node 22.11 不满足仓库要求，使用既有独立 Node 24.19，不改全局配置。初装可选原生依赖缺失导致 Vinext/SSR 失败，保留记录并修复测试环境，不删除锁文件或放宽断言。慢速 Chromium 下载已停止；本机 Edge 与 CI Chromium 分开标记。专用 API/前端端口在浏览器测试后已回收。
- 本批尚未部署、未改真实账号/网络/密钥/知识文件，不执行真实模型、封面或微信。下一步先签收本批 CI，再继续 A04/A06 和全计划；不能将六条发布旅程说成全部产品 E2E 或企业交付验收。
- 最终前端环境重新使用 Node 24.19 执行 `npm ci --include=optional` 恢复锁定版本；lint、无增量 TypeScript、Next 生产构建、Vinext 构建/2 项 SSR 均通过。按 Next 内置文档改用生产 standalone server（复制必需静态文件），再次运行六条旅程 **6 passed，27.2 秒**；浏览器网络仅允许两处本机测试端口。这是最终本地浏览器结果，不复用前面开发服务器的 25.9 秒或 `next start` 的中间结果。
- 本批源码 **`0d6608fdfec2d071676c1892e633bd8c15d761e9`** 已普通推送；作者仍为 John Wang/heee000 noreply。仅触发一次 [CI 35858597117](https://github.com/heee000/ContentFlow/actions/runs/35858597117)，已核对 SHA：前端（含新增 Chromium）和 SBOM 成功，后端真实服务测试仍在运行，尚不能整体签收。继续只查同一 run，不重复 dispatch；无 PR/合并/部署。
- 后续按同一 run 最终签收：**四项全部 success；623 passed / 7 warnings / 226 subtests，218.37 秒，覆盖率 84.56%**。真实 PostgreSQL/MinIO及新增四种并发均通过；Linux Chromium 生产 standalone 的六条旅程 **6 passed，32.9 秒**，2 项 SSR、生产构建、当次 Python/npm 已知漏洞扫描、SBOM/来源签名验证均通过。最后只补文档签收，源码证据对应 `0d6608f`；不代表漏洞不存在、已部署或企业交付完成。继续 A04/A06 等余项。

## 2026-09-24 审核收尾、指标边界与测试初始化

- 对应 [审核契约](content_review_contract.md)、[文本传输](text_transport_security.md)、[指标数值](metrics_validation_contract.md)、[启动/测试隔离](runtime_initialization_contract.md)。本批处理 A04/A06/A38/A36/A42/A43 及 A15 夹具稳定性，完整审计的其他编号继续开放。
- 审核绑定内容版本与所有可编辑字段指纹；编辑/审批执行本地规则，不触发收费 AI。旧评分不冒充新评分；旧证据、编辑后证据、人工决定分开保存。警告审批必须明确确认并说明理由；no-op 不升版，不重复排素材。共享内容锁等待后重新载入，审核/素材/审计异常时同事务回滚。
- 前端脏稿、进行中保存、远端冲突、丢保存回执均阻止误批旧稿；离开需确认，不把草稿恢复能力冒称为 A17 已完成。A37 共享 Cookie 工作区错投未解决。
- 文本请求禁止五类重定向，独立 opener 不改系统代理/urllib 策略；15 项离线真实跳转机制用例验证只有第一跳，敏感异常链不外露。
- 指标先有真实失败回归（字符串 Infinity 返回 201），再增加严格有限范围、Worker 共用校验和两项数据库约束。旧异常只隔离不删改，汇总显示排除数/不完整，不产生建议。前端指标服务失败不会让其他队列空白。A33 累计量叠加、状态/窗口/计数定义仍未修。
- 默认导入不加载 Settings/.env、不创建引擎/目录；显式 factory 启动取代 `contentflow.api:app`。pytest 和浏览器 API 在导入前隔离继承配置，明确 TEST 目标保留。Alembic 已提供 connection 时不重复取 Settings，独立 Worker 不误用别的配置。
- 中间后端：初始化/审核/私有部署 60 passed；指标/迁移/三项初始化 86 passed / 7 warnings，53.58 秒。后端全套首轮 **719 passed / 1 failed / 34 skipped / 7 warnings / 226 subtests，275.89 秒**；失败是自有 Worker pool 未显式释放使 Windows 无法删临时 DB，不是权限/ACL 故障。新增 Worker context/close，只释放自有 pool；借用 factory 不被关闭，永久循环退出自动释放；保留原素材清理断言并增生命周期回归后重验。
- 浏览器首轮原 17 条通过；加入指标场景后第一次因 Next 自带 `alert` 与业务错误区域同名而选择器失败，限定真实业务提示后完整 **19 passed，42.0 秒**（本地 Edge，生产 standalone）。没有修改业务门禁绕过断言。前端 lint、无增量 TypeScript、Vinext 构建/2 项 SSR 通过，Ruff/diff 检查通过；最终后端/源码 CI 下文另记。
- 本批 PG 新增四种审核/编辑竞争及六项指标边界/NaN/旧数据迁移测试。本机未提供专用服务目标，skip 不算通过。新 head `c7d8e9f0a1b2`，至少 34 表；回退审核表会丢归档、回退指标会丢隔离标记，真实操作必须先备份并协调版本，不自动迁移实机。
- 未部署、未调用真实模型/Embedding/媒体/微信、未改网络/账号/密钥/真实内容/受保护知识文件。阶段同步仅普通 commit/push 现有分支，作者身份已复核 heee000；不 force、不合并、不开放 PR。下一阶段先 A37/A08，再 A05/A07/A09/A12 和内容质量/运行验收，不以本批关闭完整目标。
- 生命周期修复后专项 **45 passed / 25 subtests，32.20 秒**；最终本地后端全套 **721 passed / 34 skipped / 7 warnings / 226 subtests，246.56 秒**。Ruff、暂存差异检查通过；47 个明确源代码/测试/文档文件，不含知识文件、配置秘密或测试产物。PG 用例在同步前固定不同捕获时刻，避免依赖时钟微秒分辨率；本机仍跳过，必须下列确切 CI 签收。
- 本批源码 **`d6596d5a4a5a5cbd5172b833f40387a7897abdbb`** 已普通推送当前功能分支，作者 John Wang/heee000 noreply。仅触发一次 [CI 35892709913](https://github.com/heee000/ContentFlow/actions/runs/35892709913)，返回 SHA 已核对，最初状态 queued；未签收前不称 CI 通过。继续只查询此 run，不因列表延迟重复 dispatch。没有 PR/合并/部署。
- 最终按同一 run 签收：**四项全部 success；755 passed / 7 warnings / 226 subtests，334.54 秒，覆盖率 84.87%**。包含真实 PostgreSQL/MinIO、新增四种审核竞争、六项 PG 指标边界/NaN/历史无损隔离；约束拒绝测试中的 PostgreSQL ERROR 是预期负例，整体测试无失败。前端 **19 passed（Chromium，46.2 秒）**、2 项 SSR、生产构建及当次 Python/npm 已知漏洞扫描、SBOM/签名来源验证全部通过。最后仅补文档签收，不再触发同一源码 CI；未部署，不代替实机恢复、真实平台或企业交付验收。

### 下一阶段预检：A37/A08（当时尚未实施；当前在制状态见下节）

- 已重新沿 `web/lib/contentflow-api.ts → dependencies.get_principal → auth.switch/refresh` 读链路：页面数据批次没有上下文代次，分页可跨切换继续，401 自动重试仍用原请求却读取最新 Cookie；`refreshPromise` 只在一个标签页内共享。必须同时处理请求投递和迟到响应，单独广播“已切工作区”不足以构成正确性保证。
- 先增加双标签页真实失败回归：标签 B 切工作区后标签 A 创建、上传/下载、排期/确认、轮询/分页及退出；验证旧页面不写入新工作区、不覆盖新视图、不静默丢稿。区分用户有权访问但错投与跨租户越权。
- 设计候选是服务端强制比对浏览器的预期身份/工作区上下文、前端固定每次请求/分页批次的上下文并丢弃旧响应；不能只信任可省略的 `Session-Mode` 声明，也不能影响正常服务端 Bearer 身份校验。登录/bootstrap/媒体读取等入口要逐项定义兼容边界，不能只修一处表单。
- 刷新需跨标签页协调，并覆盖 refresh 与切工作区交错、回执丢失、无 Web Locks/存储失败的安全退路。保留被盗旧 refresh token 的重放撤销保护，不通过宽泛接受旧令牌来掩盖并发问题。具体协议与验收尚未定稿，不把本预检当成实现。

## 2026-09-24 当前工作树第二次全链路复审（审计轮）

- 用户重新要求审视全项目，本轮以 HEAD `3444609` 加既有未提交会话代码为对象，仅审计、隔离验证和记录，没有继续业务修改。完整结论见 [当前工作树复审](project_audit_20260924_followup.md)。
- A37/A08 已有上下文前置条件、跨页刷新协调、旧响应隔离及保留输入实现，不能沿用上一节“尚未实施”。接回原会话专项 **6 passed / 21.6 秒**，完整本地浏览器 **25 passed / 54.0 秒**；后端 **735 passed / 34 skipped / 7 warnings / 226 subtests，250.22 秒**。Ruff、lint/无增量 TypeScript、diff 检查通过。PG 跳过不算签收，当前候选未取得新 CI/部署证据。
- 仍复现 A05 同键不同 run、A33 累计 100→150 求和 250、A34 重复/空平台、A35 非法 index 被接受、A39 非图片字节 ready、A40 标签禁用词漏检、A41 空 schema ready。A42 Infinity 已被 422 拒绝，不再作为当前未修漏洞。
- 新增 A44：队列唯一键冲突恢复会回滚调用者整笔事务；探针模拟首次查重 miss，实际 SQLite 冲突后返回已有 Job，但之前业务更新丢失，真实 PG 可达入口尚需专项。新增 A45：活动外平台重生成返回 202，最终 awaiting_review 但 0 内容。新增 A46：通过运行校验的 production/禁用 Mock 配置仍可由构造器 override 选择 Mock，属于策略一致性缺陷，不是匿名越权/跨 Provider Eval 冒认。
- A16 从代码风险加深为两个临时 SQLite 会话复现：三秒未提交更新晚于同步查询提交后，增量结果 0 条、全量可见。当前两秒回看不是提交游标。会话迟到响应浏览器负向断言还需等待处理完成屏障，25 条绿色不替代该测试质量修正。
- 图片探针最初漏合成媒体配置，修正夹具后复现；畸形 Bearer 正确返回 401，不列漏洞。无真实外发、数据/网络/秘密操作，受保护知识文件未读改/未暂存。本轮无暂存/提交/推送/CI/部署；完整目标继续开放，先收尾会话候选，再做幂等/执行权/费用/内容可信与运行验收。

## 2026-09-24 最新审计断点：会话收尾仍在制，新增生成检查点缺口

- 用户最新要求全项目复审，本轮不继续业务实现。当前汇总入口为 [全项目当前审计](project_audit_20260924_current.md)，优先于历史报告中的候选测试状态。HEAD 仍为 `3444609`；全部既有会话/测试/文档改动保留。
- 接回已启动的后端全套：**761 passed / 38 skipped / 7 warnings / 226 subtests，324.40 秒**。前端构建完成，但 `npm test` **7 passed / 1 failed**：源码常量已移至 api-config，旧测试仍断言位于 API 客户端。不是生产配置保护消失的证明，也不能算全绿。
- 接回会话浏览器：**2 passed / 1 failed / 5 未执行**；迟到响应的 `Response.finished()` 屏障仍超时，移除 Content-Length 后没有解决，先前根因推测不能当定论。另选此前未执行的刷新/切区交错与 localStorage 拒绝两项，**2 passed / 12.8 秒**；不是完整 27 条通过。真实 PG 四项新并发仍跳过，候选无新 CI/部署。
- 新 A47：实际 Mock Agent 已完成第一平台非空文案，注入第二平台 TimeoutError 后真实 Worker 最终 failed，保存内容 0、run 结果空。缺按平台/阶段持久检查点；合成故障、不含真实费用，不能说生产调用账本也完全丢失。
- 再次隔离复现 A05/A16/A28/A33/A34/A35/A39/A40/A41/A44/A45/A46；A42 Infinity 继续 422。Ruff、lint/无增量 TypeScript、diff 检查通过；npm 当前在线审计 0 已知漏洞，Python 锁文件审计 `No known vulnerabilities found / exit 0`。完整证据看上述报告，不沿用旧 SHA 的绿色；233 个源码/测试/交付候选文件前后哈希 0 变化，不含知识文件。
- 首次探针的网络拦截误挡 Windows 事件循环回环连接、沙箱临时目录权限和 pip-audit 子进程编码属于测试环境问题；没有改 ACL/依赖，唯一空临时目录已核对清理。无真实账号、业务库、外部模型/媒体/微信、网络/密钥或保护知识文件操作，无暂存/提交/推送/CI/部署。完整目标仍未完成，下一次实施先收尾候选与前述门禁。

## 2026-09-24 A37/A08 会话候选收尾实施

- 持续目标恢复后先完成已有在制代码，未叠加无关新功能。Cookie 业务入口必须携带登录会话/用户/工作区 HMAC 上下文；不依赖可省略的 Session-Mode。缺少 428、不匹配 409，Bearer 仍按明确身份鉴权。session/bootstrap 只用于恢复前置条件；历史 refresh 重放仍撤销，不为多页方便放宽安全规则。
- 前端固定 API/context/epoch，分页不继承新身份，响应头及 JSON/Blob 完成后拒绝旧结果；跨页 Cookie 变更使用同源 Web Locks，同页共用续期 Promise，锁内先探测避免重复轮换。失回执/无锁/上下文变化保留输入并停止自动操作，明确确认后重载；不声称实现关页恢复或跨来源/设备锁。
- 补齐 40 项上下文/入口负向用例及 4 项真实 PG 行锁竞争。认证接口锁后 populate_existing 复核，覆盖缓存旧身份等待切区、同旧 refresh 并发保留重放撤销、正常续期后切区。最后本地会话专项 **50 passed / 4 skipped / 1 warning，45.47 秒**；4 项 PG 本地跳过，必须本批 CI，不能算通过。后端代码自上轮全套 **761 passed / 38 skipped / 226 subtests，324.40 秒** 后未再变动，不将专项重跑写成另一轮全套。
- 第一项前端失败是源码位置断言未跟随模块拆分。修正为检查 api-config 和 API 客户端重导出，保留正式模式目的地限制；新增实际 TS 模块行为测试，验证持久化/显式 override 在正式模式均不能改变请求目的地。
- 第二项已完成有界定位：迟到 GET 返回 200，注入 JSON 长度正确，但未消费响应未结束。独立本地拦截矩阵中，Edge 对 response Cache-Control:no-store 的未读 body 不报告 requestfinished；页面/上下文级路由均如此，去掉该响应头则结束。不是断言 UTF-8、后台页或 Content-Length 是根因，也不推断所有浏览器都相同。新加两个响应释放回归先为 **7 passed / 2 failed**，实现清理后通过。
- 对拒绝的迟到响应、401 业务响应和恢复探测，尽力取消未读 body；取消失败不替代上下文错误，不重放服务端动作。测试等待成功或明确取消的请求终态，并由实际模块 Promise 终结断言补足“不继续分页”，没有删除等待或固定 sleep。Playwright 本地客户端只在 requestfinished 解析 Response.finished，因此不能只用该方法等待已取消请求。补充同步抛错/异步拒绝的清理失败和无锁不重试回归。
- 最终本地：完整生产 standalone **27 条 Edge 浏览器通过，58.2 秒**；Vinext 构建成功，12 项实际 TS 模块 + 2 项 SSR **14/14**，lint/无增量 TypeScript、Ruff/diff 检查通过。浏览器只连接临时 API/SQLite，不运行 Worker、真实平台或费用调用。当前真实 PG/MinIO、Chromium、同 SHA CI 尚待签收，后续追加确切 SHA/run，不沿用旧 CI。
- [会话协议](browser_session_contract.md) 记录一规范前端来源、Web Locks/localStorage 退路、在途合法 A 操作不能撤销、API/Web 协调升级和回退。没有新 DB 迁移；旧 Cookie 客户端遇到新 API 会 428。仍需统一候选交付，不自动部署 Ubuntu，不更改网络/秘密，不复用旧真实调用授权。
- 当前索引已从过时的 A37“尚无保护”改为已实施/待目标环境签收；审计原失败与 A44–A47 证据保留。Git 作者/登录账号复核为 John Wang / heee000 noreply / heee000；下一步按历史阶段同步授权普通 commit/push 当前功能分支，仅一次 dispatch 新源码 CI，不 force、不合并/PR/部署。受保护知识文件未读、未哈希、未改、未暂存；完整目标继续开放。
- 会话源码 **`12eaf9a4bd3135da7ddea4079004d49b41225e9f`** 已普通推送，作者 John Wang/heee000 noreply。仅触发一次 [CI 35904373071](https://github.com/heee000/ContentFlow/actions/runs/35904373071)，实际返回的 head SHA 已核对，触发时 in_progress；没有因列表延迟或等待而重复 dispatch。
- 最终同一 CI **四项全部 success**。后端 **799 passed / 7 warnings / 226 subtests，369.89 秒，覆盖率 84.96%**，包含真实 PG/MinIO 和新增 4 项会话行锁竞争，本机的 skip 由该 SHA 的真实服务测试补齐。Linux Chromium **27 passed（1.2 分钟）**，模块/SSR **14/14**、生产构建、当次 Python/npm 已知漏洞审计、可复现源码/SBOM 和签名来源验证均通过。最终仅追加文档签收，不重发同一源码 CI，不合并/部署；不代替真实模型、平台、容量、异机恢复或公网安全验收。

### 下一阶段预检：A05 生成意图与回执（尚未实施）

- 已沿页面 `CampaignsView.run()` → `routers/runs.create_run()` → `WorkflowRun` → `enqueue_job()` 复核：页面 POST 空 body，Run 只有唯一 trace_id，没有调用者操作 ID；Job 按新建 run.id 去重。每次 HTTP 重发会新建 Run，当前按钮 busy 状态不是服务端幂等或丢回执恢复。
- 新意图需有工作区范围的持久操作编号、规范化请求摘要和 DB 唯一约束。原样重放应返回同一 Run/Job/审计回执，编号复用但活动/平台/provider 等请求不同必须冲突；先识别回执，再检查会变化的活动/Prompt/style 状态，不能让已成功请求因为后续改稿而无法核对。明确“核对旧请求”和“主动新生成”的不同操作，不静默生成新编号重试。
- 前端需在请求发出前保存按 API/用户/工作区隔离的意图，回执丢失后保留并提示核对，刷新后可原样重放；存储不可用时不能假称具备恢复保障。覆盖双页、同编号并发、已提交后丢回执、异请求冲突、跨工作区/身份和主动再次生成的实际 API/浏览器测试。
- 请求摘要不等于 A09 完整执行配置快照，也不防不同编号的反复调用费用；后者仍靠 A12 预算/并发门禁。A44 队列函数会整事务 rollback，设计新幂等恢复不得照搬；需明确事务所有权和真实 PG 竞争。A45 目标平台子集、A46 Mock 策略可同阶段按独立失败回归收尾，但不能绕过安全核对，A47 检查点不得盲重放未知供应商调用。
- 以上仅为基于源码的下一阶段设计边界，不算已实现或验收。先签收本批会话候选再修改下一批代码。

## 2026-09-24 A05/A45 生成接受与恢复实施

- 上一轮为有效进展：会话修复及 CI 已签收。本轮接手 HEAD `16c26bf`，只有受保护知识文件未跟踪；没有重置或读取它。开始下一阶段生成幂等，不重复会话审计。
- 先新增真实 API/SQLite 失败回归，原实现 **13 failed / 2 passed**：重复接受、异请求/活动复用编号、缺少/非法编号、旧 Brief 及无效目标。新增工作区范围 `generation_intents` 接受回执，强制 Idempotency-Key 和 expected_campaign_updated_at，锁后复核，已有回执在可变治理检查前返回。不同意图必须新编号，旧请求恢复不自动换号。
- Run、回执、队列与审计原子接受；先 flush 自有 Run 确保 SQLite 外层写事务，再在 savepoint 内插回执，冲突只删除本次尚未接受的 Run。没有复制 A44 的整 Session rollback。覆盖审计异常无残留和受控查重 miss 后实际唯一冲突；另有五项真实 PG 并发/锁后缓存刷新测试，必须本批 CI，不以本机跳过签收。
- A45 API 拒绝活动外/重复平台，Worker 在 Provider/Embedding/策划之前复核历史队列目标，不再先规划后假成功。活动创建/编辑全部字段限制仍属 A34；A46 Mock 策略未顺带宣称修复。
- 网页先按 API/用户/工作区保存原始意图，读回确认后才发 POST；30 秒写请求截止/网络错误/未知回执都保留编号，刷新只恢复提示、不自动重发。核对匹配回执后清除；同一回执由不同组件生命周期先后收到时幂等清理，不能误删新意图。服务端仅在确认没有接受回执且旧 Brief 前置失效时返回专门代码，前端据此允许重新准备。
- 新只读 `GET /generation-intents/{request_id}` 可在不执行生成的情况下查询，跨工作区无回执返回 404；不能把查询时 404 当成证明在途请求永不提交。生成记录显示完整任务/操作编号，审计保存编号。浏览器 sessionStorage 不承诺关页/跨设备持久恢复，A17 仍开放。
- 新迁移 **d8e9f0a1b2c3**，至少 **35 表**；只添加回执表，不伪造历史 Run 回执。迁移首轮发现新表被错误算为旧 schema 必需表，且原识别逻辑用动态 HEAD 会跳过新表；改为增量表与确切旧版本，并验证新表主键/唯一/外键。保留旧数据及隔离回退，新旧无版本采用、不完整约束拒绝均有测试。没有操作真实数据库，生产回退删除回执会丧失去重能力，必须协调备份和停止重试。
- 专项中间 **35 passed / 2 failed**：一个新测试错误取 app.state.settings（实际配置是显式依赖覆盖），另一个为上述迁移缺口；修正后迁移/生成旧套 **41 passed / 7 warnings，24.35 秒**。首次完整后端 **784 passed / 43 skipped / 7 warnings / 226 subtests，380.43 秒**；随后补不完整约束拒绝、只读回执和明确回执回滚断言，最终专项结果另记，不能写成已重跑新全套。
- 浏览器生成四项首轮 **4 passed / 21.4 秒**，原完整 31 条通过；加入离页回执乱序后完整 **32 passed（1.2 分钟）**。十项回执模块与原会话/SSR 合计 **24/24**，lint、无增量类型、生产构建、Ruff 通过。最终编号显示另做生成专项，结果及同 SHA CI 随后补记。无真实模型、媒体、社媒、收费、Ubuntu 部署或网络/秘密操作。
- [生成意图契约](generation_intent_contract.md) 已写明 API/Web/Worker/schema 协调升级、旧客户端 422、只读查询/接受重放区别、历史任务和回退风险。现有 API/Worker/治理测试明确用新操作辅助函数，栈验收脚本同步；没有通过给所有测试自动塞随机编号来掩盖幂等回归。

## 2026-09-24 生成候选的全项目复审（仅审计）

- 用户最新要求重新审视项目。本轮接手 HEAD `16c26bf` 及全部 A05/A45 在制改动，保留业务/测试代码，只补 [最新全项目审计](project_audit_20260924_generation_review.md) 和状态索引；没有阶段提交、推送、新 CI 或部署。
- 重新完成当前完整后端 **785 passed / 43 skipped / 7 warnings / 226 subtests，340.88 秒**；生产 standalone Edge 浏览器 **32/32（1.2 分钟）**；模块/SSR **24/24**，Vinext 构建、ESLint、无增量 TypeScript、Ruff/diff 检查通过。不是沿用此前 784/43 的中间结果；43 跳过仍不算真实 PG/MinIO 签收。
- 新 A48：SQLite 中确认发布时注入审计异常，HTTP 500 后仍留下 queued PublishJob，却没有分发 Job；同请求重放 202，仍 0 Job/0 接受审计。根因是首次写在 SAVEPOINT、外层实际事务未开启；A05 新路径已处理，旧发布路径未处理。没有证明 PG 出现同样行为，也没有真实发布。
- 新 A49：实际发布回执 TS 模块把损坏 JSON/拒绝读取当作 null，缺模式等字段的记录仍接受；写前读回与身份匹配清除未统一。属于恢复链路问题，同编号服务端幂等未失效，未声称已在浏览器/平台造成重复发布。
- A07 新故障注入：策划后标记失租，真实 Worker 仍调用 Mock generate/review；末尾停止不够。A47 第一平台完成 107 字后第二平台失败，最终 0 内容/空结果；A39 12 字节非图片 ready、A40 标签禁用词漏检、A33 100+150=250、A35 非法 index、A34 重复/空平台、A41 空 schema ready、A44 队列整事务回滚、A46 Mock override、A16 晚提交漏增量均重新验证。A45 正确拒绝无效生成目标，A42 Infinity 仍 422，不重复算未修。
- npm 当前在线扫描 0 已知漏洞；Python 扫描被自动审批拒绝（发送私有依赖名/版本到在线漏洞服务未获授权），没有绕过，不写成通过。只读复核旧 CI `35904373071` 仍 completed/success/head=12eaf9a；不用于当前未提交候选。
- 首次构建 EPERM 和探针过短 objective/误取 id 是环境/夹具问题，修正后补验；未改权限或依赖。243 个源码/测试/交付候选前后 SHA-256 **0 变化**；真实账号/库/模型/媒体/微信、Ubuntu、网络/秘密及受保护知识文件均未触碰。
- 后续先收尾 A05/A45 候选及 A48/A49/A44 事务与恢复，再执行权/预算/Mock 策略和检查点，随后内容可信/质量、指标与体验、同版恢复/通知/容量。公开部署计划仍冻结，完整目标仍 active，历史真实调用授权不复用。

## 2026-09-24 发布事务候选全项目复审（只审计）

- 接手时保留 A05/A45、A44/A48 与新 A49 失败测试；不再沿用“事务完全未修”。`atomic_insert.py` 不提交/回滚调用者事务，12 项本地接受故障回归通过；新 3 项 PG 接受/队列竞争本机跳过。
- 当前完整后端 **796 passed / 1 failed / 46 skipped / 226 subtests，352.81 秒**。新 A50：SQLite 重新加载后发布 scheduled_at 无时区；首次/重放均 202 同任务，但本机 Asia/Shanghai 日期解释与显式 UTC 差 -8 小时。不是已证明实际调度偏移或 PG 故障，不能修改断言掩盖 API 契约。
- 32 条 Edge production standalone 浏览器全过（1.2 分钟），默认模块/SSR 24/24、构建/Ruff/lint/TypeScript/diff 通过；单独发布回执模块 **3 failed**，文件尚未进默认 npm test。绿色总数不能关闭 A49。
- 重验 A07 失租后继续 generate/review，A39 非图片 ready、A40 标签漏检、A33 累计快照相加、A35 非法索引、A34 Brief、A16 晚提交、A46 Mock override。A47 本轮错误发生在第二平台 generate：0 可编辑稿，result_json **保留 ai_provenance**，没有 plan/contents；不笼统写全部证据丢失。A45/A42 拒绝仍正确。
- 最新完整记录见 `project_audit_20260924_acceptance_review.md`。247 个源码/测试/交付候选哈希 0 变化；仅文档更新，没有同步失败候选、部署、真实调用、Ubuntu/网络/秘密或受保护知识文件操作。在线 Python 审计授权仍待明确，不通过别的路径绕过。
- 下一实施入口：A50/A49 收尾并接入正式测试；完成 A44/A48/A05/A45 当前 PG/同版验收，再执行权/预算/检查点、成品质量和运行验收。整体目标未完成。

## 2026-09-24 发布恢复与时间契约收尾（本地验证，未交付）

- 持续目标恢复后保留 A05/A45 和 A44/A48 在制实现，补齐 A49/A50，不撤回原子性修复、不删失败断言。详细问题、方案、限制和升级要求见 [发布接受契约](publication_acceptance_contract.md)；旧审计失败保留为历史证据。
- A50：新增 `UTCResponseModel`，ORM 与有 datetime 字段的非 ORM 响应统一输出带 Z 的 UTC；naive SQLite 结果按既有 UTC 存储约定规范化，带偏移输入保持同一时刻，可空字段保持 null。新增纯模型和实际首次接受/重放/列表回归；初始六项失败后修复。不改历史数据、不修改输入校验，也不推断原 Worker 调度已偏移。
- A49：发布意图独立严格解析，损坏、不可读、不完整不再等同 absent；发请求前保存并读回，收到回执核对操作/稿件/渠道，清理比较当前存储身份，不能由迟到旧响应删除新意图。新增同步在途门禁、30 秒截止及已接受但列表刷新失败的独立提示；超时不会取消服务器操作。
- 新只读 `GET /publishing/intents/{request_id}` 按工作区返回原任务，不排队、不修旧孤立任务；404 不是在途请求永不接受的证明。queued/scheduled 的分发身份缺失返回 409；响应和列表显示操作编号但不返回完整 request_json 或 token。旧客户端/旧 API 与新 Web 的兼容限制明确记录。
- 页面提供只读核对、原样重放、重新读取和显式人工处置。人工清除要求确认已核对服务器及平台，明确不取消在途任务；不可读时禁清除，损坏记录须是刚核对的原始快照。此入口依赖人的核对，不声称服务器已经证明无副作用或提供跨设备持久恢复。
- 正式 `npm test` 接入 **16 项**实际 TS 发布回执测试，与生成、会话、SSR 合计 **40/40**，Vinext 构建通过。发布浏览器恢复专项 **13 passed，55.3 秒**；再补 UTC、上海、纽约夏季/冬季的四条真实日期输入与显示旅程，完整生产 standalone Edge **43 passed，1.8 分钟**。测试为临时 API/SQLite，没有 Worker、真实平台或费用调用。
- 完整后端 **806 passed / 46 skipped / 7 warnings / 226 subtests，330.63 秒**。全套收集后仅新增一项所有声明时间的响应模型覆盖检查，并给 PG 用例补回执/只读查找断言；最终相关三文件 **10 passed / 3 skipped / 1 warning，3.76 秒**。这不是重新跑了 807 项全套，3 项 PG skip 也不算通过。补跑首次被沙箱临时 SQLite 权限拒绝，审批后同命令通过，未改 ACL。
- 最后 Ruff、ESLint、无增量 TypeScript 通过；本地 Docker Linux engine 管道不存在，未启动或改配 Docker/网络，无法取得本批真实 PG/MinIO 证据。已一次询问在线依赖包名/版本查询授权，尚无回复，不通过 CI 间接绕过被拒绝的外发。没有重用历史 CI 绿色。
- 当前 HEAD 仍 `16c26bf`，本批源码与记录尚未暂存/提交/推送，未触发新 CI、迁移实机或部署。真实数据库、现有待审稿、账号/秘密、代理/DNS、受保护知识文件未操作。完整目标仍开放：候选服务级验收后继续 A07/A12/A46/A47；A39/A40/A35/A41 等阻断公开测试的缺口不因本地绿色关闭。

## 2026-09-24 A07 领取身份、调用与业务写入保护

- 前一轮完成了候选补验和记录，属于有效进展；本轮没有再次停留在审计。先新增九条真实 Worker/隔离数据库故障回归，旧实现 **9 failed**：策划后失权仍继续生成/审核，以及四种 Session 写法仍提交旧状态。修复先收尾主调用链，再补真实适配器与迁移，不改变既有人工核对门禁。
- 新 `ExecutionFence` 绑定领取的 job/workspace/worker/attempt/lease_token 和有效期。文本、Embedding、媒体生成/轮询、搜索、下载、平台每次 HTTP 与对象存储操作前检查；媒体跳转逐跳检查。业务 Session 的 ORM/批量/原始 SQL/commit 和独立进度事务使用同一门禁，条件 UPDATE 在业务事务内取得 Job 锁，锁等待后再检查有效期；完成和错误传播同样隔离旧执行。
- 复核发现人工重试重置 attempts，仅 Worker ID/次数不足以构成执行身份。新增独立领取令牌及真实重新领取/旧令牌拒绝测试；初版该场景仍失败，再补不可复用身份。SQLite 双读者领取改为候选状态条件更新，只有一方获得新身份，不因两个旧 SELECT 都看见 queued 而双领。
- Heartbeat 不再续活已过期租约；领取令牌、有效期是续租/完成/失败的必填参数，不能悄悄退回旧检查。时间在等待锁之后重新校验。API/Web 没有获得修改令牌的入口，供应商原有幂等键不变。
- 调用账本开始受门禁，完成证据使用专门 Session；迟到成功保存 `late_succeeded` 与 hash/用量，未知继续 `outcome_unknown`。不继续使用迟到结果生成/改业务状态；不把“已返回调用”冒充业务已完成，也不自动重复扣费、清理或补发。已发送请求和 SDK 在途操作不能撤回，A12/A47 仍开放。
- 新迁移 **e9f0a1b2c3d4**（接 d8e9f0a1b2c3，仍至少 35 表）：历史任务保留且不伪造旧 running 的领取令牌，新增列采用/隔离回退与每次换令牌已有测试，恢复脚本同步。必须备份、排空/停止旧 Worker 并协调同版升级，不能混跑不校验令牌的旧 Worker；没有操作真实库或 Ubuntu。
- 初版执行权/队列/调用账本 **40 passed / 9 subtests（20.69 秒）**，Worker/存储/Prompt 旧套 **49 passed / 8 subtests（29.94 秒）**。加入令牌后队列与执行专项 **50 passed / 9 subtests（28.28 秒）**；最终执行权/迁移 **40 passed（22.76 秒）**，当前 API/新迁移的完整生产 standalone Edge **43 passed（1.9 分钟）**。最终完整后端与同步事实在本节后续追加，不把中间回归当最后源码全套。
- 测试夹具原有一篇发布稿，首轮补验总数误计；改为只核对本次 run，保留不生成旧结果断言。另两个新测试构造错误（JSON 冒号被当 SQL 参数、连接器类名大小写）分别改参数绑定/确切名称，均不是产品漏洞，未删断言。Ruff 通过；新四项 PG 行锁/等待过期专项已收集但跳过，不能称通过。
- 远端分支与本地 HEAD 均为 `16c26bf`，作者 John Wang/heee000 noreply、GitHub 登录 heee000 已只读确认。既有阶段同步授权仅用于普通 commit/push 当前分支，不 force/PR/合并/部署；已检查工作流，功能分支 push 不触发 CI。在线依赖元数据外发授权尚无回复，不手动触发含该审计的 CI 或绕过限制。阶段 SHA 和最终验证结果随后追加。
- 最终后端完整回归 **847 passed / 50 skipped / 7 warnings / 226 subtests，383.04 秒**；50 个服务用例跳过不算通过。完整 Edge **43/43（1.9 分钟）**，Ruff、diff、前端 lint/无增量类型已通过；本轮不更新真实 PG/MinIO、远程覆盖率或在线漏洞扫描结论。不沿用上一批 806/46 作为本批全套，没有变更断言来掩盖业务失败。
- 最终前端默认模块/SSR **40/40**、Vinext 构建也已终结通过；本轮后端/浏览器/前端验证进程均已结束。下一步按现有授权普通同步阶段源码，保持“本地通过、真实服务/CI 待签收、未部署”的明确标签。

## 2026-09-24 存储候选全项目复审（仅审计，未继续实现）

- 用户最新要求全面审视项目。本轮 HEAD `fe6d0e314be5f1edbcb840e226cadd58bafa1a3d`，接手已有 A51 暂存/短事务/回收 UI、f0a1b2c3d4e5 迁移和两份新测试，完整保留。最新报告 [存储候选、成品可信度与交付缺口](project_audit_20260924_full_reassessment.md)，旧汇总页明确划分当前入口/历史证据，避免叠加多个“最新”。
- A51 候选已先持久记录 charged staging 再外部写入，失权/回滚留下可核对对象，管理员按来源终态/冷却/无引用确认精确回收；不再将旧慢存储失败冒充当前结果。真实 PG/MinIO、kill/响应丢失/回收竞争、回收 UI 浏览器与当前迁移恢复仍待验。
- 接回专项 **68 passed / 1 warning / 8 subtests，48.66 秒**；本轮完整后端 **859 passed / 50 skipped / 7 warnings / 226 subtests，456.24 秒**，完整 production standalone Edge **43/43（1.9 分钟）**，默认模块/SSR **40/40**，构建/Ruff/lint/无增量 TypeScript/diff 通过。所有验证进程结束，不留待重启的句柄；50 个 skip 不算服务级通过。
- 新 A52：实际 Worker + 独立线程 API 改稿，旧素材先 stale，生成返回后又 ready；最终 asset v1/content v2 needs_review，8094 字节已存。现有发布版本门禁仍有效，本轮没有证明误发旧封面。后续需外部返回后按 content→asset 锁序重新检查并补正式竞争回归，不能只改 UI 标签。
- 当前再验 A39（12 字节非图片 ready）、A40（禁词只在标签仍过规则）、A47（第二平台失败后 0 可编辑稿、保留 ai_provenance）、A33（累计 100/150 汇总 250）、A35（非法/重复/bool 向量 index 接受）、A41（空库 readiness 200）、A34/A46/A16/A28；其余资源预算/RAG/配置快照/体验和运维缺口分开标注静态证据与待验收风险。未声称完成渗透测试或发现 P0 利用链。
- 238 个源码/测试/交付文件审计前后哈希一致，仅补文档。无暂存、提交、推送、新 CI、真实调用/费用、部署、网络或凭据操作，受保护知识文件仍不读/哈希/修改/暂存。本机 Docker engine 不可用，不启动服务，在线审计外发边界不变。先收尾当前候选与同版验收，再按报告顺序推进；完整产品目标仍未完成。

## 2026-09-24 A52 素材版本与异步结果隔离实施

- 持续目标恢复，本轮实施而非重复审计。保留 A51 已有短事务/staging/回收候选，先新增实际 Worker + 独立线程真实改稿 API 失败回归，旧实现 **13 failed / 3 passed，11.55 秒**；发现生成/轮询/搜索成功和失败均可能覆盖 stale，PUT 期间还有第二个竞争窗口。
- 新增 `asset_work.py`，操作捕获内容版本、来源/输入、异步 task id/原 URI/状态；外部阶段之间无写锁重读，最终按执行 Job→父内容→素材取锁后复核再提交。图库下载原先跨 PUT 的内容锁移到最终写回，不通过延长锁或撤销执行权保护掩盖竞争。混合候选 selected 不作为取消条件，最新选择在写回时保留。
- 确定过期结果由 Worker 回滚业务事务，终结原任务并保存 `outcome=superseded` 和审计，不自动重发；已经上传的对象保持 charged staging，不挂到新稿、不自动删除。远程抛错仍保留未知/失败处置，不借改稿宣称取消成功；失败回写不覆盖 stale 或新替换素材。详见 [素材异步结果契约](asset_work_contract.md)。
- 前端队列明确显示“旧结果未采用”和存储核对说明，不将其标成生成成功，不提供重放按钮。新增真实临时 API/生产页面浏览器用例；无真实 Provider/费用调用。A52 无新增迁移，组合候选仍用 A51 的 f0 head。
- 初版相邻回归 **55 passed / 1 failed / 31 subtests**，唯一失败为旧 Mock Session 不支持新重读所需的 `info`/上下文；夹具补齐语义，保留异步 Provider 指纹原断言。随后素材/存储/Worker/执行权/PG 既有集 **123 passed / 31 skipped / 39 subtests（69.54 秒）**。新 3 项 PG 行锁/真实 Worker 回归已加入但本机无目标，必须后续真实运行，不记为已验收。
- 全量后端与前端/浏览器验证已启动，最终结果在本节追加。未改真实数据、账号、网络或受保护知识文件；未部署、未触发在线依赖审计或 CI。旧审计 A52 失败保留为修复前证据，当前只能称本地候选，整体目标继续开放。
- 最终完整后端 **880 passed / 53 skipped / 7 warnings / 226 subtests，397.24 秒**；53 跳过包括新 3 项 PG，用例存在不等于已通过。前端 lint/无增量 TypeScript、Vinext 构建、默认模块/SSR **40/40**、Ruff/diff 检查通过。
- 新浏览器测试首轮因误定位桌面不可见的移动导航下拉框超时，**5 passed / 1 failed / 38 未执行**；页面快照明确显示桌面折叠导航，改为实际展开“资源与系统”并进入任务队列，未删结果断言或跳过。完整 production standalone Edge 重跑 **44 passed（1.8 分钟）**，包含旧结果提示且无重放按钮。所有验证进程均已结束。
- Git 作者复核为 John Wang / `182348029+heee000@users.noreply.github.com`；远端该分支与本地同步前均为 `fe6d0e3`。按历史阶段同步授权，仅普通提交/推送现有功能分支；CI 仅 main push/PR/手动触发，构建和部署仅手动触发，本轮不触发这些流程，不绕过在线依赖审计外发限制。确切阶段 SHA 随签收追加。
- 本批源码提交 **`9a1da9994567a888e91d465f31029b4111474871`**，作者 John Wang/heee000 noreply，包含 A51/A52、迁移、回归和审计记录共 28 文件；本条签收追加仅文档，不改变上述测试对应的实现。提交后只剩受保护知识文件未跟踪，未纳入提交；远端同步结果以本轮普通 push 回执及分支 HEAD 为准。仍无当前真实 PG/MinIO/CI/部署签收。
