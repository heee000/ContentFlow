# ContentFlow 持续改进实施进度

更新：2026-09-23。目标：按 [全面复审计划](project_audit_20260921.md) 逐项实施并验证，保持私人测试范围；不以本次已完成的局部工作替代完整目标。真实付费调用、内容审批/发布及高影响权限迁移仍按具体授权推进。

## 当前阶段

最新复审见 [2026-09-23 再次全链路复审](project_audit_20260923_followup.md)：该轮只审计，新增 A30–A35。持续目标随后恢复实施，A30/A31/A32 的本地安全修复已通过最终回归，见 [渠道安全契约](channel_security_contract.md) 和本页末节。没有部署；A33/A34/A35 与完整计划继续保留，不能将本次安全阶段当作整个目标完成。

2026-09-23 的复审补充 A25–A28 后，已按持续目标恢复实施；详见 [复审快照](project_audit_20260923.md)。A01/A02/A03/A27/A29 已普通推送为 `942faa5`，真实服务 CI 四项全部通过（480 passed / 218 subtests，覆盖率 83.61%）；应用尚未部署到 Ubuntu。随后已实现 A25 服务端发布物绑定；最终预览/指纹确认及 A26 在制代码现在随 `a1e828f` 一并同步，仍缺专门验收，不能整体关闭。再做 A04/A06 所见即所批与审核版本、A05 幂等及后续阶段。不能把局部修复当作完成整个目标。

| 项目 | 当前状态 | 验收/后续 |
| --- | --- | --- |
| A01 素材未知门禁 | 已实现共享领域检查、关联历史风险、API 锁顺序和审核者核对入口 | 本地与真实 PostgreSQL 并发 CI 通过；部署与双页面交互验收待签收 |
| A02 人工/AI 来源 | API 保留显式来源；Worker 在人工/其他 provider 配置漂移时不调用供应商、明确失败；来源矩阵和新增专项通过 | CI 通过；更完整的模型/端点快照仍属 A09；部署待签收 |
| A03 严格模型输出 | 已实现策划/成稿/审核契约 v1、错误脱敏、原调用证据保留和 Eval 契约版本门禁 | 相关专项、本地全套及真实服务 CI 通过；真实新版契约需部署后受控评测，旧六例不得直接当作新证据 |
| A27 下载锁顺序 | Worker 与 API 已统一先锁内容再锁素材；新增真实 PostgreSQL 下载/改稿竞争测试 | 本地与真实 PostgreSQL CI 通过；部署待签收 |
| A25 最终发布物 | 已补确认凭证/权限/过期/改图字节回归和真实页面预览门禁 | 本地 API/浏览器验收通过；本批确切源码的 PG/CI 待签收，未部署 |
| A26 发布幂等 | 修复签名未绑定操作 ID，以及并发等待锁后未先重查回执；新增故障矩阵 | 本地失回执/刷新/重放通过；新增四种 PostgreSQL 竞争待本批 CI，未部署 |
| A28 去除 Demo 领域硬编码 | 待实施 | 中立 CTA/规则修复/视觉方向，多行业回归 |
| A29 存储扫描时钟边界 | 已复现并修复；固定/回拨时钟、单页/分页均保留真实缺失检测 | 存储 18 项、本地全套及 CI 通过；部署待签收 |
| A04 未保存审批 | 待实施 | 编辑/保存/审批绑定同一版本，浏览器旅程验证 |
| A05 生成幂等 | 待实施 | 用户操作 ID、摘要、唯一约束、并发重放 |
| A06 审核版本 | 待实施 | 失效旧审核、规则重跑与前端过期标识 |
| A07/A08/A09/A12/A14 调用可控 | 待实施 | 租约、跨页刷新、Prompt 快照、预算和未知处置语义 |
| A10/A11/A13 内容质量 | 待实施 | 相关性/引用/索引版本、批处理、真实任务与人工质量评测 |
| A15/A22/A23 运行验收 | 部分测试资产已有，目标环境待签收 | E2E、异机恢复、通知、峰值与当前主机封面/微信链路 |
| A16/A17/A24 体验与可维护性 | 本进度页提供当前状态入口，其余待实施 | 按需加载、URL/草稿、模块拆分及版本事实对齐 |
| A18/A19/A20/A21 扩大开放的安全边界 | 尚未实施，不自动恢复公网计划 | 数据生命周期、DB 最小权限/RLS、身份密钥、网络出口；高影响项需明确授权与恢复前置 |
| A30/A31/A32 新安全缺口 | 修复已普通推送为 `a1e828f`：受控错误/日志、固定官方目标、严格平台配置、历史错误响应隔离 | CI 四项通过，578 passed、84.33%；未部署；历史实机日志及凭据影响尚未核查 |
| A33/A34 新正确性缺口 | 本轮隔离 API 复现，未修复 | 累计指标/作品数口径、PATCH 未提交与 null 的区分 |
| A35 Embedding 响应对应关系 | MockTransport 复现重复/越界 index 被接受，未修复 | 严格索引类型、唯一性、完整输入覆盖；异常不替换可用索引 |

## A01/A02 本轮设计要点

- 所有用户发起的素材重试、来源切换、候选下载、已有素材替换、内容重新审批和通用 Job 重试复用检查。未解决人工核对、活动任务、未核对的生成/轮询账本分别处理。
- 风险记录跟随同一内容的素材历史，正文改版不能把旧 unknown 藏起来；活动任务只约束当前素材，正常混合候选可并行。
- API 统一先锁 ContentItem，再锁 Asset，随后锁 Job；等待后重新读取，防止旧状态通过。新增真实 PostgreSQL 的重试/切来源竞争测试。
- `request-manual-review` 仅审核者可用，只把已终止素材生成/轮询失败转入核对，不声称已核实，也不调用供应商；核对后复用原 Job 重试，不另开双份任务。历史 attempt 原样保留。
- 下载/搜索是只读操作，不将其失败误当作新的生成费用；保留媒体契约的同键自动恢复，人工重新操作仍受门禁保护。
- 修正复审的幂等证据表述：本地账本键与实际媒体生成幂等键不同，新 Job 并不必然导致重复扣费；供应商履约仍需外部验证。
- 来源测试覆盖 12 种 manual/generate/search/hybrid × manual/http/mock，以及人工上传后编辑、人工视频来源和 AI 配置缺失；无真实媒体调用。
- 原有完整流程测试依赖“默认 manual 却自动生成”的旧缺陷，现明确声明 `image_source=generate`，保留实际生成/导出断言，不删断言来制造通过。

## 验证记录

- 素材/候选/核对首轮：17 passed / 14 subtests；扩展来源用例后 manual 模块 14 passed / 14 subtests。
- Ruff、前端 ESLint、Next.js 生产构建/类型检查及 Vinext/2 项 SSR 测试通过。第一次 Next 构建受沙箱 `.next/trace` 写权限限制，正常权限下同命令通过；未修改 ACL。
- 初次全套：398 passed / 18 skipped / 215 subtests，1 个依赖旧默认来源行为的流程测试失败；明确测试来源后，复跑最终结果为 **401 passed / 18 skipped / 215 subtests passed**（含后续增加的来源用例）。
- 本机 Docker daemon 未运行，PostgreSQL/MinIO 测试本地跳过；跳过不能算通过，待 CI 取得真实服务证据。
- 没有变更真实数据库、密钥、代理、当前待审稿或部署。受保护知识文件不读不改。

## 2026-09-23 后续实施补充

- 素材专项：38 passed / 17 subtests；覆盖排队后的 http/manual/mock 配置漂移，确认 Worker 在建 Provider 前拒绝、任务终止失败且原来源保留。
- 三阶段输出/Eval/Worker 专项：81 passed / 2 subtests。后续增加了 Eval 契约版本门禁，进入全套回归。
- 不把 `"false"`、整数 1 当 bool；不把 NaN/Infinity 变成 10 分；缺字段、非法引用结构和平台布局会失败。详情见 [输出契约](model_output_contract.md)。
- 测试与生产一致性修正：候选夹具使用生产 `candidate_optional` 名称；Mock 公众号在没有事实/必含信息时给出非空、明确人工确认的段落摘要。静音镜头允许空口播，不为测试通过捏造语音。
- 重要升级约束：旧真实 Eval 仍保留但没有契约 v1 证据；部署后新生成需另行受控评测。当前未部署、不重跑真实模型、不代替用户审批现有稿件。
- 扩展全套初轮为 456 passed / 19 skipped / 218 subtests，另有 1 个存储核对失败。没有以单独重跑通过掩盖它：新增固定/回拨时钟 × 单页/分页四个失败回归，修复已见对象的扫描标记严格晚于扫描起点；存储 18 项通过。
- 修复后的最终本地全套：**461 passed / 19 skipped / 7 warnings / 218 subtests passed，104.44 秒**。19 项跳过不当作真实服务通过；警告来自现有测试客户端兼容层和 SQLite 日期适配。真实 PostgreSQL 并发及 MinIO 等以本轮提交的 CI 结果另行签收。
- `942faa5` 的 [GitHub CI 35844426448](https://github.com/heee000/ContentFlow/actions/runs/35844426448) 四项全部通过：**480 passed / 7 warnings / 218 subtests，163.06 秒；覆盖率 83.61%**。Python/npm 当次依赖扫描未报告已知漏洞，前端生产构建、SBOM/来源签名校验通过；不等于渗透测试、实机部署或真实平台验收。

## 2026-09-23 A25 服务端发布物绑定

- 新增 [发布物绑定契约](publish_manifest_contract.md)。新任务必须素材就绪且具备工作区内 URI/SHA-256/大小；记录正文、选用素材和渠道配置摘要。Worker 检查后固定脱离 Session 的副本，实际读取素材时核对同一批发送字节。
- 官方、脚本与导出共用；安全重试/脚本回退不能重新捕获不同发布物；无 manifest 的旧排队任务拒绝，已提交/终态仍先返回，不触发重复调用。
- 专项首轮 38 passed / 8 subtests；随后本地全套 **470 passed / 20 skipped / 7 warnings / 226 subtests，108.10 秒**。之后追加微信 MockTransport 字节校验测试并把换图夹具改为不同文件，最终相关专项 **39 passed / 8 subtests，21.86 秒**；没有实际网络请求。Ruff、diff 检查及前端 lint 通过。本批最终 CI 另行签收，不复用上一提交结果。
- 新增 PostgreSQL 排期/选图竞争用例；本机跳过，必须 CI 签收。测试夹具补齐真实临时存储对象/校验和，脚本失败夹具保留原 request_json，仅改所模拟的字段，不删生产校验制造通过。
- 尚未实现最终预览与确认指纹，不能保证用户看到的页面就是服务端捕获的版本；A25 仍部分完成。所有修改均未部署，真实待审稿和微信未触碰。
- `f399c0f` 首次 CI `35846018526`：490 passed / 1 failed / 226 subtests，覆盖率 83.92%。失败为既有素材重试/切源竞争用例把所有 Job 总数断言为 1；夹具升级成真实存储对象后，切源胜出还会正确排队一个旧文件 `storage.delete`。保留两个入口必须一胜一拒及仅一个生成任务断言，新增清理任务仅在切源胜出时出现、引用确切旧对象/owner 的校验；不以重跑偶然胜出掩盖问题。此次前端与 SBOM 已过，但整体失败、来源签名跳过，修正后须新 CI 签收。
- 最终源码验证提交 **`69d27ac622acefa3abd1bada5f5f71661efc1c25`**，已普通推送。[CI 35846656631](https://github.com/heee000/ContentFlow/actions/runs/35846656631) **四项全部通过：491 passed / 7 warnings / 226 subtests，171.24 秒，覆盖率 83.92%**。包含新增 PostgreSQL 排期/选图竞争及真实 MinIO；前端 lint/SSR/生产构建、当次 Python/npm 安全扫描、SBOM 和来源签名校验通过。另本地素材/存储专项 33 passed / 17 subtests。最后只补文档签收，不把该 CI 冒称为部署或浏览器交互验收。

## 2026-09-23 A30/A31/A32 安全边界实施

- 先新增 50 项正式回归，旧实现全部失败：非法 bool、任意渠道地址、微信鉴权/上传/草稿/提交/查询阶段的 HTTP/网络/JSON/平台错误及显式重定向。采用 MockTransport，无真实网络/平台调用。
- 新增分平台封闭配置，新请求严格校验，旧渠道在连接测试/预览/发布加载/运行时重新校验。微信只有 JSON true 才公开提交；页面显示异常配置而非安全草稿提示。官方 Origin 固定、所有请求禁止跟随重定向，不保留测试域名例外。
- 连接器仅提供受控阶段/类别/数字状态码，去掉原响应/异常链；敏感 httpx 请求日志不输出 token URL。Worker 四类平台任务统一兜底，历史错误不原样返回，但数据库证据不删除。安全诊断仍可显示 HTTP 状态和白名单提示。
- 保持外部写入前安全重试、写入后结果不确定人工对账的区别；新增安全异常同样进入原未知结果门禁。无效旧配置不排队/不发网络，不自动修正配置或更新旧发布清单。
- 扩展覆盖抖音、API→Worker→viewer、错误数据库/日志、历史证据保留、正常真假语义、实例存活期间配置变化及任务删除竞态。现有标量发布 ID 兼容保留；不接受对象/布尔型 ID 或数字 token。
- 中间验证：安全＋原连接器/清单 60 passed；安全/API/Worker/脚本流 115 passed / 8 subtests；扩展安全专项 86 passed。首轮全套 556 passed / 20 skipped / 7 warnings / 226 subtests，140.62 秒；随后补充的最终源码仍需本页最后签收，不复用中间绿色。Ruff、前端 lint/TypeScript、diff 检查通过。
- 最终源码本地全套 **558 passed / 20 skipped / 7 warnings / 226 subtests，181.66 秒**；安全专项＋原连接器/素材清单 **97 passed，6.96 秒**。Ruff、前端 lint、TypeScript、Next.js 生产构建、diff 检查通过。20 项跳过不算真实 PG/MinIO 通过；本轮没有更新覆盖率或在线依赖扫描结论。中间 556/557 次全套后有补充保护/标量 ID 兼容，以上最终结果才对应定稿源码。
- 现有 A25/A26 在制代码已完整保留。已核对依赖齐全、作者 John Wang/heee000 noreply 和登录账号 heee000，按阶段同步授权普通同步当前功能分支快照，明确标注在制，不以提交替代最终预览/幂等专项、PG 并发和浏览器验收。不是 GitHub 登录或强推故障；无 force、合并、真实费用、部署、网络/密钥修改。完整目标继续 active。
- 实际源码提交 **`a1e828f1dedf9826dd0441c2d0332a1a441437c0`** 已成功普通推送到 `codex/enterprise-media-runtime`，没有开放 PR、合并或部署。仅触发一次 [CI 35854697647](https://github.com/heee000/ContentFlow/actions/runs/35854697647)，已按该确切 ID 复核为 **in_progress**（源码 SHA 一致），尚不能写成通过。继续时查询同一 run，不因列表短暂未显示而重复 dispatch。最后仅补本记录，CI 的源码证据仍指向 `a1e828f`。
- 后续按同一 run 签收：四项全部 success，**578 passed / 7 warnings / 226 subtests，170.95 秒，覆盖率 84.33%**；含真实 PostgreSQL/MinIO，前端构建、安全扫描和来源证据通过。该结论只对应 `a1e828f`，不覆盖下面的新源码。

## 2026-09-23 A25/A26 确认协议与浏览器验收

- 新建 41 项确认专项。先确认失败证据：签名版本 bool/float 被接受；预览不要求操作编号、原签名换编号可创建第二份任务。修复为严格版本和签名绑定操作编号，同步更新验证脚本与旧夹具。
- 原路径字节变化测试首轮错用内容寻址的 `put()`，实际生成新 URI；没有将夹具错误算作漏洞。改为只在临时夹具中损坏确切对象，覆盖同大小篡改/变大/缺失均被预览读取拒绝。最终发送仍由既有逐字节校验保护，不宣称确认事务提前读取了所有素材。
- 同键回执重放优先于过期/当前状态校验；新增内容锁等待后的回执重查。四种 PostgreSQL 测试覆盖同请求、不同模式、不同内容和胜者提交前改稿，断言仅一份新 PublishJob、一个分发 Job、一次确认审计；本机未配置 PG，必须本批 CI 签收。
- 本地确认＋渠道安全专项 **128 passed / 21 skipped，25.59 秒**；最终后端全套 **599 passed / 24 skipped / 7 warnings / 226 subtests，199.34 秒**。24 项跳过不算真实服务验证。Ruff、前端 lint/TypeScript、Next 生产构建和 diff 通过；SSR 与最终 CI 另行记录，不混用结果。
- 新增 Playwright 1.63.0 锁定依赖及 [浏览器验收入口](browser_acceptance.md)。真实 Next/API/临时 SQLite、无 Worker，六条 Edge 无头旅程 **6 passed，25.9 秒**；确认前封面/勾选门禁、服务端入库后丢回执与刷新原样重放、改稿冲突、存储拒绝、素材失败和跨账号隔离均通过。首轮是测试 `getByLabel` 包含 option 文本导致定位失败，改为真实无障碍 combobox 名称；未修改产品来绕过失败。
- 本机默认 Node 22.11 不满足仓库要求，使用既有独立 Node 24.19，不改全局配置。初装可选原生依赖缺失导致 Vinext/SSR 失败，保留记录并修复测试环境，不删除锁文件或放宽断言。慢速 Chromium 下载已停止；本机 Edge 与 CI Chromium 分开标记。专用 API/前端端口在浏览器测试后已回收。
- 本批尚未部署、未改真实账号/网络/密钥/知识文件，不执行真实模型、封面或微信。下一步先签收本批 CI，再继续 A04/A06 和全计划；不能将六条发布旅程说成全部产品 E2E 或企业交付验收。
- 最终前端环境重新使用 Node 24.19 执行 `npm ci --include=optional` 恢复锁定版本；lint、无增量 TypeScript、Next 生产构建、Vinext 构建/2 项 SSR 均通过。按 Next 内置文档改用生产 standalone server（复制必需静态文件），再次运行六条旅程 **6 passed，27.2 秒**；浏览器网络仅允许两处本机测试端口。这是最终本地浏览器结果，不复用前面开发服务器的 25.9 秒或 `next start` 的中间结果。
